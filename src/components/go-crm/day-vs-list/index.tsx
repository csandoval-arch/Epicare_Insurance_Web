"use client";

import React from "react";
import CalendarConcept from "./CalendarConcept";

export default function DayVsListContainer() {
  // El usuario solicitó explícitamente "que parezca un calendario".
  // Eliminamos el Debug Panel para mostrar directamente la solución arquitectónica final.
  return <CalendarConcept />;
}
